- Change site so user cannot access unpublished chapter or book by guessing the id in the URL. 
    For this, change catalog:views.py so the function for details() and chapter() filters out unpublished works (only accepts publish dates of the past, not blank nor future dates)

- To add...